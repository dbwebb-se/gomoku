test:
    pre:
        - make install
        - make check

    override:
        - make test

notify:
  webhooks:
    # A list of hook hashes, containing the url field
    # gitter hook
    - url: https://webhooks.gitter.im/e/40729fae34ddda1346cc
